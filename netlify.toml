[build]
  command = "npm run build"
  # Next.js plugin handles publish directory automatically

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  # Use legacy peer deps to handle React version conflicts with Clerk
  NPM_FLAGS = "--legacy-peer-deps"
